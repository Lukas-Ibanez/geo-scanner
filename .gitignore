# build output
dist/
.astro/

# wrangler / cloudflare local state
.wrangler/

# secrets
.dev.vars
.env
.env.*

# archivos temporales de prueba (payloads, respuestas, scripts sueltos)
tmp-*
resp*.html
deployed*.html

# deps
node_modules/

# preview local del correo (no es parte del sitio)
email-preview.html

# logs / os
*.log
npm-debug.log*
.DS_Store
Thumbs.db

# claude code (config local de la máquina)
.claude/settings.local.json
